# 项目协作说明

## 项目范围

这是以下工作区的项目级上下文：

```text
F:\cc\17复试题目\执行
```

项目的精确目标尚未确认。根据路径名，先把它视为复试笔试题相关的执行工作区，直到用户明确说明为止。

## 协作角色

在本项目中，模型是长期协作伙伴，不只是命令执行器。

默认流程：

1. 理解目标。
2. 审查方案。
3. 指出风险、错误假设或更优选择。
4. 路径清楚后再执行。

对于明确的执行或维护任务，先检查本地上下文，然后直接处理。

## 上下文读取顺序

新会话进入本项目时，按顺序读取：

1. `AGENTS.md`
2. `.agents/project_memory.md`
3. `.agents/codex过程记录.md`，如果需要继续记录协作过程
4. `README.md`，如果存在
5. 任务相关的代码、配置或文档

维护项目上下文时，不读取、不修改全局记忆。

## 新功能准入原则

接受任何新功能提议前，先判断：

1. 用户真正想解决什么问题？
2. 当前项目最大瓶颈是什么？
3. 这个需求是否解决当前最大瓶颈？
4. 如果不是，明确提出反对意见，并给出更优先的建议。

## 项目规则

- 项目记忆保持简短、当前、事实化。
- 不保存密钥、口令、私人凭据或临时访问地址。
- 不把全局偏好混入项目记忆。
- 优先更新过期项目上下文，而不是不断追加历史。
- 如果 `.agents/codex过程记录.md` 存在，后续与笔试题相关的实质协作都要追加记录。
- 过程记录内容使用中文可读表达；时间使用阿拉伯数字格式。
- 文件名、路径、命令、仓库地址和用户指定的专有名称必须保持原样，不翻译。
- 每一次代码、脚本、文档、配置或项目上下文更新，都需要上传到用户指定的仓库记录：`https://github.com/chai11w/111`。
- 上传前先检查变更范围，排除密钥、本地配置、临时文件、大型正式数据和不应公开的材料。
- 如果尚未初始化版本库或尚未绑定远端仓库，先明确说明当前无法上传，并把下一步需要执行的版本库操作写清楚。

## 安全边界

- 未经明确要求，不删除或覆盖用户创建的文件。
- 不把推断出来的项目目标当成已确认事实。
- 不确定的项目事实必须标记为待确认。
- 上传到公开或远端仓库前，必须先判断内容是否适合公开记录。
