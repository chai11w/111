# 项目记忆

## 当前状态

- 项目路径：`F:\cc\17复试题目\执行`
- 项目上下文系统初始化于二零二六年七月六日。
- 项目当前已经初始化为本地版本库。
- 项目精确目标尚待用户确认。
- 当前工作假设：本项目与复试笔试题执行有关，这只是根据目录名推断。
- 协作过程记录已启用，文件为 `.agents/codex过程记录.md`。
- 协作过程记录内容应以中文呈现；后续记录中除用户明确要求保留的专有文件名外，不主动写英文。
- 用户指定代码更新记录仓库：`https://github.com/chai11w/111`。
- 后续每次代码、脚本、文档、配置或项目上下文更新，都应上传到上述仓库记录。
- 当前本地版本库已绑定远端仓库。
- 远端仓库可访问，当前本地 `main` 分支已跟踪远端 `main` 分支。
- 首次项目上下文提交已推送到用户指定仓库。

## 已完成

- 创建项目级 `AGENTS.md`。
- 创建 `.agents/project_memory.md`。
- 创建 `.agents/codex过程记录.md`，用于记录完成本次笔试题时用户与模型的协作过程。
- 已把旧的英文记录文件名替换为中文文件名。
- 已在项目规则中写入后续继续记录的要求。
- 已把“每次更新都上传到用户指定仓库”的规则写入项目规则和项目记忆。
- 已初始化本地版本库，绑定用户指定远端仓库，并完成首次提交与推送。

## 未完成

- 尚未确认本项目的真实目标。
- 尚未开始真实笔试题执行流程。
- 尚无题目材料、解题脚本、数据流程或任务说明文档。

## 架构规则

- 项目记忆只保存当前事实，不保存完整对话流水。
- 稳定协作规则写入 `AGENTS.md`。
- 当前项目状态写入 `.agents/project_memory.md`。
- 按时间顺序的协作过程写入 `.agents/codex过程记录.md`。
- 代码更新记录目标仓库为 `https://github.com/chai11w/111`。
- 每次准备上传前，必须先排除密钥、本地配置、临时文件、大型正式数据和不应公开的材料。
- 在项目复杂度真正上升前，不新增过多上下文文件。

## 重要命令

暂无项目专用命令。

常用检查命令：

```powershell
Get-ChildItem -Force
git status --short --branch
Get-Content -LiteralPath '.agents\codex过程记录.md' -Encoding UTF8
```

版本库与远端检查命令：

```powershell
git status --short --branch
git remote -v
git ls-remote --symref origin HEAD
```

## 已知风险

- 项目目标目前仍来自目录名推断，尚未由用户确认。
- 当前仓库已经完成首次提交和推送；后续每次项目更新仍需继续提交并推送。
- 早期对话的精确消息时间无法从界面直接读取，记录中已标注为约略时间。
- 不要强推或覆盖远端历史。
- 上传前需要判断笔试题材料、答案草稿或正式数据是否适合进入远端仓库。

## 不要做

- 不把全局偏好或全局记忆写进本项目上下文。
- 不把完整协作流水写进项目记忆，只在项目记忆保留记录文件位置和记录规则。
- 不把密钥、口令、私人凭据、大型正式数据或生成产物写进项目上下文。
- 不把推断出的项目目标说成已验证事实。
- 不在未检查远端状态时强行推送或覆盖远端历史。
- 不把不适合公开或不适合上传的笔试材料提交到远端仓库。

## 下一步

正式处理笔试题前，先请用户用一两句话确认：

- 本项目真实目标。
- 输入材料是什么。
- 期望输出是什么。
- 第一阶段最重要的交付物是什么。

后续每次发生与笔试题相关的实质协作，都应追加到 `.agents/codex过程记录.md`，并记录本地时间、用户输入和模型输出。

下一步应确认真实笔试题目标和输入材料；后续每次项目更新都要提交并推送到用户指定仓库。
